"use client";

// ─────────────────────────────────────────────────────────────────────────
// Screen 2 · Vehicle Registry (Fleet)          OWNER: Mayuri
// Mockup: "2. Vehicle Registry"
// TODO: filters (type/status/search) · Add Vehicle · table (reg no, name/model,
//       type, capacity, odometer, acq. cost, status badge). Rules: reg number
//       unique; Retired/In Shop hidden from dispatch.
// ─────────────────────────────────────────────────────────────────────────

export default function VehiclesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Fleet</h1>
      <p className="mt-1 text-muted">Vehicle registry — scaffold, not yet built.</p>
    </div>
  );
}
